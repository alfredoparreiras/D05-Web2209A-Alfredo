<UserControl x:Class="PassportApp.Views.PassportListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PassportApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <DataGrid AutoGenerateColumns="False" SelectionMode="Single" HorizontalContentAlignment="Center">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" IsReadOnly="true" Binding="{Binding Path=Id}"/>
                <DataGridTextColumn Header="First Name" Binding="{Binding Path=FirstName}"/>
                <DataGridTextColumn Header="Last Name" Binding="{Binding Path=LastName}"/>
                <DataGridTextColumn Header="Date Of Birth" Binding="{Binding Path=DateOfBirth}"/>
                <DataGridTextColumn Header="Age" IsReadOnly="true" Binding="{Binding Path=Age}"/>
                <DataGridTextColumn Header="Country" Binding="{Binding Path=Country}"/>
                <DataGridTextColumn Header="Current Location" Binding="{Binding Path=CurrentLocation}"/>
                <DataGridTextColumn Header="Traveling" Binding="{Binding Path=Traveling}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Separator Height="15"/>
        <StackPanel>
            <Label Content="Destinantion"/>
            <TextBox Width="240 "/>
        <Separator Height="15"/>
            <Button Content="Travel" Width="120"/>
        <Separator Height="15"/>
        </StackPanel>
    </StackPanel>
</UserControl>
